// const initialState = [];
// console.log(initialState);
// const featuredReducer = (state = initialState, action) => {
//   console.log(action.type);
//   switch (action.type) {
//     case "FEATURED":
//       state = action.payload.filter((e) => e.type == "featured");
//       return state;
//     default:
//       return state;
//   }
// };
// export default featuredReducer;
